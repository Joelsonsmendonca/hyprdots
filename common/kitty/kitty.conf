font_family      JetBrainsMono Nerd Font Mono
font_size        12.0

# Ctrl+C: copia se tiver selecao, senao manda SIGINT normalmente
map ctrl+c copy_or_interrupt
# Ctrl+V: cola sempre (nao conflita com nada no terminal)
map ctrl+v paste_from_clipboard

copy_on_select no
