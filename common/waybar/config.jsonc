{
    // Posição da barra (top, bottom, left, right)
    "layer": "top",
    "position": "top",
    "height": 40, // Altura da barra
    "spacing": 4, // Espaçamento entre módulos

    // Módulos da Esquerda
    "modules-left": [
        "ext/workspaces",
        "hyprland/window"
    ],

    // Módulos do Centro
    "modules-center": [
        "clock"
    ],

    // Módulos da Direita
    // battery e backlight somem sozinhos se o hardware nao existir
    // (comportamento nativo do waybar) -- por isso o mesmo arquivo serve pro PC e pro notebook
    // bluetooth agora é o ícone do blueman-applet na tray (igual o nm-applet faz pro wifi)
    "modules-right": [
        "custom/keybinds",
        "idle_inhibitor",
        "backlight",
        "cpu",
        "memory",
        "battery",
        "pulseaudio",
        "custom/notification",
        "custom/update",
        "tray",
        "custom/power"
    ],

    // --- Configurações Detalhadas dos Módulos ---

    // Usamos ext/workspaces (protocolo ext-workspace-v1), NAO hyprland/workspaces.
    // Motivo: a config do Hyprland e Lua, e nesse modo o servidor rejeita os
    // dispatches em texto ("dispatch workspace 5" -> erro de sintaxe Lua). O
    // hyprland/workspaces clica mandando exatamente esse texto, entao clicar nao
    // faz nada. O ext/workspaces ativa o workspace pelo protocolo Wayland direto.
    "ext/workspaces": {
        "format": "{icon} {name}",
        "sort-by": "number",
        "format-icons": {
            "default": "",
            "urgent": "",
            "active": ""
        },
        // all-outputs=false (padrão): cada barra mostra só os workspaces do SEU monitor.
        // Os workspaces fixos são definidos no hyprland.lua (workspace_rule persistent),
        // então no desktop a barra do LG mostra 1-5 e a do AOC mostra 6-10 sozinhas.
        "all-outputs": false
    },

    "hyprland/window": {
        "format": "{}",
        "max-length": 35
    },

    "clock": {
        "format": " {:%H:%M}",
        "format-alt": " {:%d/%m/%Y}",
        "tooltip-format": "<big>{:%A, %d de %B de %Y}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": ""
        },
        "tooltip": true
    },

    "backlight": {
        "format": " {percent}%",
        "on-scroll-up": "brightnessctl set 5%+",
        "on-scroll-down": "brightnessctl set 5%-"
    },

    "cpu": {
        "format": " {usage}%",
        "tooltip": true,
        "on-click": "kitty btop"
    },

    "memory": {
        "format": " {used:0.1f}G",
        "tooltip": true
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""],
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "tooltip-format": "{timeTo}"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": " Mudo",
        // Scroll usa o controle NATIVO do waybar (libpulse -> mesmo sink default
        // das teclas de volume via wpctl). Antes chamava `pamixer`, que NAO esta
        // instalado -> rolar o icone nao fazia nada e parecia "outro sistema".
        // max-volume 100 pra bater com o `-l 1` das teclas.
        "scroll-step": 5,
        "max-volume": 100,
        "on-click": "pavucontrol",
        "on-click-right": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "format-icons": {
            "default": ["", ""]
        }
    },

    "tray": {
        "icon-size": 18,
        "spacing": 10
    },

    "custom/keybinds": {
        "format": "",
        "tooltip-format": "Ver atalhos do Hyprland",
        "on-click": "~/.config/hypr/scripts/show-keybinds.sh"
    },

    "custom/notification": {
        "tooltip": true,
        "format": "🔔 {}",
        "return-type": "json",
        "exec-if": "which swaync-client",
        "exec": "swaync-client -swb",
        "on-click": "swaync-client -t -sw",
        "on-click-right": "swaync-client -d -sw",
        "escape": true
    },

    "custom/power": {
        "format": "",
        "tooltip-format": "Menu de energia",
        "on-click": "~/.config/hypr/scripts/power-menu.sh"
    },

    "custom/update": {
        "format": " Update",
        "tooltip-format": "Atualizar Sistema e IA",
        "on-click": "kitty --title 'System Update' bash -c /home/joelson/.local/bin/update-all"
    }

}
